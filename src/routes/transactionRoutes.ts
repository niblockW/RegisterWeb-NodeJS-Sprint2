import express from "express"; //appears on every other route file
import { RouteLookup } from "../controllers/lookups/stringLookup";
// import * as TransactionController from "../controllers/transactionController"; // NEED TO CREATE THIS FILE

function transactionRoutes(server: express.Express) {

}

module.exports.routes = transactionRoutes;
